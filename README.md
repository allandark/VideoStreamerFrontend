# Frontend
This is the frontend to the video streamer application. It servers React webpage with an Nginx server. Nginx serves as a reverse proxy server bridging the API and the frontend.

## Build
**Dependencies**
- Node.js
- Docker

**Build**
```
docker-compose build
```

```
docker-compose up
```